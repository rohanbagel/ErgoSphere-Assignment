services:
  - type: web
    name: ergosphere-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python manage.py migrate
    startCommand: python -m uvicorn config.asgi:application --host 0.0.0.0 --port $PORT
    autoDeploy: true
